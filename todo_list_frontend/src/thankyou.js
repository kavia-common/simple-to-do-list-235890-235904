/**
 * Utilities for startup console messages.
 */

// PUBLIC_INTERFACE
export function logThankYou() {
  /** Log a "thankyou" message to the browser console. */
  // Keeping the message exactly as requested.
  console.log("thankyou");
}
